# MMJ Portfolio — React + Vite + Framer Motion + React Three Fiber

A modern, animated portfolio for **Mohammad Mahaboob Jani** with motion and a lightweight 3D background.

## 🔧 Tech
- React 18 + Vite 5
- Framer Motion (animations)
- Three.js + @react-three/fiber + @react-three/drei (3D)

## ▶️ Local Dev
```bash
npm install
npm run dev
```

## 🚀 Deploy to GitHub Pages (recommended)
1. Commit and push to your repo (e.g., `mmj-portfolio`).
2. Ensure `vite.config.js` has `base: '/mmj-portfolio/'` (replace if your repo name differs).
3. Set homepage in `package.json` (optional) to `https://<your-username>.github.io/mmj-portfolio/`.
4. Run:
```bash
npm run deploy
```
This builds the app and publishes `dist/` to a `gh-pages` branch.

In GitHub → **Settings** → **Pages** → Select `gh-pages` branch → Save. Your site will be live shortly.

## 📝 Customize
- Replace `public/assets/profile.jpg` with your photo.
- Replace `public/assets/resume.pdf` with your real resume.
- Edit content in `src/data/profile.js`.

## 📄 License
MIT
